{snippetArea mc}
{* todo snippetArea neslo bez jmena
    TODO:
        Nejde pouzit dynamicky <{$snippetName} n:snippet"...", protoze se neinicializuje n:makro, zkusit vyresit jinak?
*}
{if !$snippetElement}
    {control $component}
{elseif $snippetElement == 'span'}
    <span n:snippet="$componentName">
        {control $component}
    </span>
{else}
    <div n:snippet="$componentName">
       {control $component}
    </div>
{/if}

{/snippetArea}
